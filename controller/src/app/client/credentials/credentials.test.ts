import 'mocha';
import { expect } from 'chai';

const request = require('supertest');

const prefix = 'CREDENTIAL API: ';

describe.skip(prefix + 'controller suite tests', async function() {
  it(prefix + 'should get credentials', () => {});
});
